{
  
"command_load": {
    "description": "装填指令关键词",
    "type": "string",
    "default": "装填"
  },
  "command_shoot": {
    "description": "开枪指令关键词",
    "type": "string",
    "default": "开枪"
  },
  "command_misfire_on": {
    "description": "走火开指令关键词",
    "type": "string",
    "default": "走火开"
  },
  "command_misfire_off": {
    "description": "走火关指令关键词",
    "type": "string",
    "default": "走火关"
  },
  "misfire_probability": {
    "description": "走火概率（0-1）",
    "type": "float",
    "default": 0.005
  },
  "default_misfire_switch": {
    "description": "新群默认走火功能开关状态",
    "type": "bool",  
    "hint": "true=开启，false=关闭，群内可通过指令覆盖",
    "default": false
  },
  "ban_time_range": {
    "description": "禁言时间范围（秒）",
    "type": "object",
    "items": {
      "min": {
        "description": "最小禁言时间",
        "type": "int",
        "default": 60
      },
      "max": {
        "description": "最大禁言时间",
        "type": "int",
        "default": 3000
      }
    }
  },
  "game_timeout": {
    "description": "游戏超时时间（秒）",
    "type": "int",
    "hint": "装填后未操作的自动结束时间",
    "default": 180
  }
}
