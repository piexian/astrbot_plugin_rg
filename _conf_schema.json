{
  "command_prefix": {
    "description": "插件指令触发前缀",
    "type": "string",
    "hint": "用于触发插件指令的前缀符号（如 /、! 等），留空则无需前缀",
    "default": "/"
  },
  "command_load": {
    "description": "装填子弹指令文本",
    "type": "string",
    "hint": "触发装填功能的指令（如“装填”）",
    "default": "装填"
  },
  "command_shoot": {
    "description": "射击指令文本",
    "type": "string",
    "hint": "触发射击功能的指令（如“开枪”）",
    "default": "开枪"
  },
  "command_misfire_on": {
    "description": "开启走火指令文本",
    "type": "string",
    "hint": "触发开启走火功能的指令（如“走火开”）",
    "default": "走火开"
  },
  "command_misfire_off": {
    "description": "关闭走火指令文本",
    "type": "string",
    "hint": "触发关闭走火功能的指令（如“走火关”）",
    "default": "走火关"
  },
  "misfire_probability": {
    "description": "随机走火概率",
    "type": "float",
    "hint": "取值范围 0-1，默认0.005（0.5%）",
    "default": 0.005
  },
  "default_misfire_switch": {
    "description": "新群默认走火功能开关状态",
    "type": "bool",  
    "hint": "true=开启，false=关闭，群内可通过指令覆盖",
    "default": false
  },
  "ban_time_range": {
    "description": "禁言时间范围（秒）",
    "type": "object",
    "items": {
      "min": {
        "description": "最小禁言时间",
        "type": "int",
        "default": 60
      },
      "max": {
        "description": "最大禁言时间",
        "type": "int",
        "default": 3000
      }
    }
  },
  "game_timeout": {
    "description": "游戏超时时间（秒）",
    "type": "int",
    "hint": "装填后未操作的自动结束时间",
    "default": 180
  }
}
