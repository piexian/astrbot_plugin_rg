{
  "game_settings": {
    "description": "核心玩法设置",
    "type": "object",
    "items": {
      "misfire_probability": {
        "description": "每次发言时的走火概率",
        "type": "float",
        "hint": "这是一个0到1之间的小数，例如0.005代表0.5%的概率。",
        "default": 0.005
      },
      "min_ban_duration": {
        "description": "禁言的最短时间（秒）",
        "type": "int",
        "hint": "设置一个合理的最小值。",
        "default": 60
      },
      "max_ban_duration": {
        "description": "禁言的最长时间（秒）",
        "type": "int",
        "hint": "设置一个合理的上限。",
        "default": 3000
      }
    }
  },
  "custom_texts": {
    "description": "自定义游戏提示文本（每个类别下一行一个）。如果留空，将使用插件自带的默认文本。",
    "type": "object",
    "items": {
      "misfire_descriptions": {
        "description": "走火时的场景描述",
        "type": "text",
        "default": ""
      },
      "user_reactions": {
        "description": "玩家被击中时的反应描述",
        "type": "text",
        "hint": "使用 {sender_nickname} 作为玩家昵称的占位符。",
        "default": ""
      },
      "trigger_descriptions": {
        "description": "成功开枪（击中）时的场景描述",
        "type": "text",
        "default": ""
      },
      "miss_messages": {
        "description": "开枪未击中（空枪）时的场景描述",
        "type": "text",
        "hint": "使用 {sender_nickname} 作为玩家昵称的占位符。",
        "default": ""
      }
    }
  }
}