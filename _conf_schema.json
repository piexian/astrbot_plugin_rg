{
  "misfire_probability": {
    "description": "随机走火概率",
    "type": "float",
    "hint": "取值范围 0-1，默认0.005（0.5%）",
    "default": 0.005
  },
  "ban_time_range": {
    "description": "禁言时间范围（秒）",
    "type": "object",
    "items": {
      "min": {
        "description": "最小禁言时间",
        "type": "int",
        "default": 60
      },
      "max": {
        "description": "最大禁言时间",
        "type": "int",
        "default": 3000
      }
    }
  },
  "game_timeout": {
    "description": "游戏超时时间（秒）",
    "type": "int",
    "hint": "装填后未操作的自动结束时间",
    "default": 180
  }
}
