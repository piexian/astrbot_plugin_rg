{
  "plugin_config": {
    "description": "插件配置",
    "type": "object",
    "items": {
      "misfire_probability": {
        "description": "走火概率",
        "type": "float",
        "default": 0.005,
        "hint": "随机走火的触发概率(0-1)"
      },
      "default_misfire_switch": {
        "description": "默认走火开关",
        "type": "bool",
        "default": false,
        "hint": "新群的走火功能是否默认开启"
      },
      "ban_time_range": {
        "description": "禁言时间范围",
        "type": "object",
        "hint": "设置禁言时长的范围(秒)",
        "items": {
          "min": {
            "description": "最小时长",
            "type": "int",
            "default": 60,
            "hint": "最短禁言时间(秒)"
          },
          "max": {
            "description": "最大时长",
            "type": "int",
            "default": 3000,
            "hint": "最长禁言时间(秒)"
          }
        }
      },
      "game_timeout": {
        "description": "游戏超时时间",
        "type": "int",
        "default": 180,
        "hint": "游戏自动结束的时间(秒)"
      }
    }
  }
}
